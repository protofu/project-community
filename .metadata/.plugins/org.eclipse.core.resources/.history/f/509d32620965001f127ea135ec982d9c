<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org"
      xmlns:sec="http://www.thymeleaf.org/extras/spring-security"
      xmlns:layout="http://www.ultraq.net.nz/thymeleaf/layout"
      layout:decorate="~{layouts/index}">
<head>
    <meta charset="UTF-8">
    <title>프로젝트 목록</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</head>
<body>
    <main layout:fragment="main" class="container my-5">
        <!-- 진행 중인 프로젝트 목록 -->
        <h1 class="mb-4 text-center">현재 진행중인 프로젝트 목록</h1>
        <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
            <div class="col" th:each="project : ${list}">
                <div class="card">
                    <div class="card-header bg-primary text-white">
                        <div class="d-flex justify-content-between align-items-center">
                            <h3 class="card-title" th:text="${project.name}">프로젝트 이름</h3>
                            <span class="badge bg-secondary" th:text="'팀장: ' + ${project.maintainer.username}">팀장</span>
                        </div>
                        <div class="mt-2 text-muted">
                            <cite title="Started At" th:text="${project.startAt}">시작일</cite> ~ 
                            <cite title="Ended At" th:text="${project.endAt}">종료일</cite>
                        </div>
                    </div>
                    <div class="card-body">
                        <button 
                            class="btn btn-info btn-sm float-end mb-2" 
                            data-bs-toggle="modal" 
                            th:attr="data-bs-target='#myModal-' + ${project.id}">
                            Info
                        </button>
                        <div class="modal fade" th:id="'myModal-' + ${project.id}" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
                            <div class="modal-dialog modal-dialog-centered modal-dialog-scrollable">
                                <div class="modal-content">
                                    <div class="modal-header">
                                        <h5 class="modal-title" th:text="${project.name}">프로젝트 이름</h5>
                                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                    </div>
                                    <div class="modal-body">
                                        <div class="d-flex justify-content-between mb-3 text-muted">
                                            <cite title="Started At" th:text="${project.startAt}">시작일</cite> ~ 
                                            <cite title="Ended At" th:text="${project.endAt}">종료일</cite>
                                        </div>
                                        <p class="text-wrap" th:utext="${project.description}">프로젝트 설명</p>
                                        <button type="button" class="btn btn-primary mt-3" id="membersPopoverBtn-${project.id}" data-bs-toggle="popover" data-bs-html="true" title="Members">Show Members</button>
                                        <script>
                                            document.addEventListener('DOMContentLoaded', function () {
                                                var membersBtn = document.getElementById('membersPopoverBtn-${project.id}');
                                                new bootstrap.Popover(membersBtn, {
                                                    content: `
                                                        <ul>
                                                            <li th:each="member : ${project.members}" th:text="${member.username}">Member Name</li>
                                                        </ul>
                                                    `,
                                                    html: true,
                                                    placement: 'bottom'
                                                });
                                            });
                                        </script>
                                    </div>
                                    <div class="modal-footer">
                                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">닫기</button>
                                        <form th:if="${!myProjectList.contains(project)}" action="/myproject/join" method="POST" class="d-inline">
                                            <input type="hidden" name="projectId" th:value="${project.id}" />
                                            <button class="btn btn-success">Join Project</button>
                                        </form>
                                        <form th:if="${myProjectList.contains(project)}" action="/myproject/leave" method="POST" class="d-inline">
                                            <input type="hidden" name="projectId" th:value="${project.id}" />
                                            <button class="btn btn-danger">Leave Project</button>
                                        </form>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="card-footer text-muted">
                        <div th:if="${user.id} == ${project.maintainer.id}" class="d-flex justify-content-end">
                            <form action="/project/delete" method="POST" class="me-2">
                                <input type="hidden" name="id" th:value="${project.id}" />
                                <button class="btn btn-danger btn-sm" type="submit">Delete</button>
                            </form>
                            <form action="/project/edit" method="GET" class="me-2">
                                <input type="hidden" name="id" th:value="${project.id}" />
                                <button class="btn btn-warning btn-sm">Edit</button>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
            <form action="/project/add" method="GET" class="d-flex justify-content-end">
                <button class="btn btn-success btn-sm">Create</button>
            </form>
        </div>

        <!-- 참여 중인 프로젝트 목록 -->
        <h1 class="mt-5 mb-4 text-center">참여 중인 프로젝트 목록</h1>
        <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
            <div class="col" th:each="project : ${myProjectList}">
                <div class="card">
                    <div class="card-header bg-primary text-white">
                        <div class="d-flex justify-content-between align-items-center">
                            <h3 class="card-title" th:text="${project.name}">프로젝트 이름</h3>
                            <span class="badge bg-secondary" th:text="'팀장: ' + ${project.maintainer.username}">팀장</span>
                        </div>
                        <div class="mt-2 text-muted">
                            <cite title="Started At" th:text="${project.startAt}">시작일</cite> ~ 
                            <cite title="Ended At" th:text="${project.endAt}">종료일</cite>
                        </div>
                    </div>
                    <div class="card-body">
                        <button 
                            class="btn btn-info btn-sm float-end mb-2" 
                            data-bs-toggle="modal" 
                            th:attr="data-bs-target='#myModal-' + ${project.id}">
                            Info
                        </button>
                        <div class="modal fade" th:id="'myModal-' + ${project.id}" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
                            <div class="modal-dialog modal-dialog-centered modal-dialog-scrollable">
                                <div class="modal-content">
                                    <div class="modal-header">
                                        <h5 class="modal-title" th:text="${project.name}">프로젝트 이름</h5>
                                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                    </div>
                                    <div class="modal-body">
                                        <div class="d-flex justify-content-between mb-3 text-muted">
                                            <cite title="Started At" th:text="${project.startAt}">시작일</cite> ~ 
                                            <cite title="Ended At" th:text="${project.endAt}">종료일</cite>
                                        </div>
                                        <p class="text-wrap" th:utext="${project.description}">프로젝트 설명</p>
                                    </div>
                                    <div class="modal-footer">
                                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">닫기</button>
                                        <form action="/myproject/leave" method="POST" class="d-inline">
                                            <input type="hidden" name="projectId" th:value="${project.id}" />
                                            <button class="btn btn-danger">Leave Project</button>
                                        </form>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>
</body>
</html>
