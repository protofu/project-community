<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org"
      xmlns:sec="http://www.thymeleaf.org/extras/spring-security"
      xmlns:layout="http://www.ultraq.net.nz/thymeleaf/layout"
      layout:decorate="~{layouts/index}">
<main layout:fragment="main" class="container my-5">
    <h1>현재 진행중인 프로젝트 목록</h1>
    <div class="table-responsive">
        <table class="table table-striped table-bordered">
            <thead>
                <tr>
                    <th>프로젝트 이름</th>
                    <th>설명</th>
                    <th>시작일</th>
                    <th>종료일</th>
                    <th>관리자</th>
                    <th>액션</th>
                </tr>
            </thead>
            <tbody>
                <tr th:each="project : ${list}">
                    <td th:text="${project.name}">프로젝트 이름</td>
                    <td th:text="${project.description}">설명</td>
                    <td th:text="${project.startAt}">시작일</td>
                    <td th:text="${project.endAt}">종료일</td>
                    <td th:text="${project.maintainer.username}">관리자</td>
                    <td>
                        <a th:href="@{/project/detail/{id}(id=${project.id})}" class="btn btn-info btn-sm">상세보기</a>
                        <a th:href="@{/project/edit/{id}(id=${project.id})}" class="btn btn-warning btn-sm">수정</a>
                        <a th:href="@{/project/delete/{id}(id=${project.id})}" class="btn btn-danger btn-sm">삭제</a>
                    </td>
                </tr>
            </tbody>
        </table>
    </div>
</main>
</html>
