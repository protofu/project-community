<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org"
      xmlns:sec="http://www.thymeleaf.org/extras/spring-security"
      xmlns:layout="http://www.ultraq.net.nz/thymeleaf/layout"
      layout:decorate="~{layouts/index}">
<head>
    <!-- Bootstrap CSS 추가 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Custom CSS 추가 -->
    <style>
        body {
            background-color: #f8f9fa; /* 페이지 배경 색상 */
            font-family: Arial, sans-serif; /* 글꼴 설정 */
        }
        .container {
            max-width: 900px; /* 컨테이너 최대 너비 설정 */
        }
        .form-card {
            background: #ffffff; /* 폼 카드 배경 색상 */
            border-radius: 15px; /* 카드 모서리 둥글기 */
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* 카드 그림자 */
            padding: 2rem; /* 카드 내 여백 */
            margin-top: 2rem; /* 카드 상단 여백 */
        }
        .form-card h2 {
            margin-bottom: 1.5rem; /* 제목 하단 여백 */
            color: #343a40; /* 제목 색상 */
            text-align: center; /* 제목 중앙 정렬 */
        }
        .form-label {
            font-weight: bold; /* 레이블 텍스트 굵게 */
            color: #495057; /* 레이블 색상 */
        }
        .form-control {
            border-radius: 0.375rem; /* 입력 필드 모서리 둥글기 */
            border-color: #ced4da; /* 입력 필드 테두리 색상 */
        }
        .form-control:focus {
            border-color: #80bdff; /* 입력 필드 포커스 시 테두리 색상 */
            box-shadow: 0 0 0 0.2rem rgba(38, 143, 255, 0.25); /* 입력 필드 포커스 시 그림자 */
        }
        .btn-custom {
            background-color: #007bff; /* 버튼 배경 색상 */
            border: none; /* 버튼 테두리 제거 */
            color: #ffffff; /* 버튼 텍스트 색상 */
            font-weight: 600; /* 버튼 텍스트 굵기 */
        }
        .btn-custom:hover {
            background-color: #0056b3; /* 버튼 호버 시 배경 색상 */
            color: #ffffff; /* 버튼 호버 시 텍스트 색상 */
        }
        .btn-custom:focus, .btn-custom:active {
            box-shadow: none; /* 버튼 포커스 및 활성화 시 그림자 제거 */
        }
        .text-center {
            text-align: center; /* 텍스트 중앙 정렬 */
        }
        .form-group {
            margin-bottom: 1.5rem; /* 폼 그룹 하단 여백 */
        }
    </style>
</head>
<body>
    <main layout:fragment="main" class="container">
        <div class="form-card">
            <h2>안녕하세요, <span th:text="${user.username}"></span> 님! 프로젝트를 만드시나요?</h2>
            <form action="/project/add" method="POST">
                <input type="hidden" name="maintainerId" th:value="${user.id}" />
                
                <div class="form-group">
                    <label for="name" class="form-label">프로젝트 이름</label>
                    <input type="text" id="name" class="form-control" name="name" placeholder="프로젝트 명" required>
                </div>

                <div class="form-group">
                    <label for="description" class="form-label">프로젝트 설명</label>
                    <textarea id="description" class="form-control" name="description" placeholder="프로젝트 설명" rows="5" required></textarea>
                </div>

                <div class="form-group">
                    <label for="startAt" class="form-label">프로젝트 시작일</label>
                    <input type="date" id="startAt" name="startAt" class="form-control" required>
                </div>

                <div class="form-group">
                    <label for="endAt" class="form-label">프로젝트 종료일</label>
                    <input type="date" id="endAt" name="endAt" class="form-control" required>
                </div>

                <div class="text-center">
                    <button type="submit" class="btn btn-custom">제출</button>
                </div>
            </form>
        </div>
    </main>

    <!-- Bootstrap JS 추가 -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
