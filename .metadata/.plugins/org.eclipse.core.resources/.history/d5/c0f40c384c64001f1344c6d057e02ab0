<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org"
      xmlns:sec="http://www.thymeleaf.org/extras/spring-security"
      xmlns:layout="http://www.ultraq.net.nz/thymeleaf/layout"
      layout:decorate="~{layouts/index}">
<main layout:fragment="main" class="container my-5">
    <h1>현재 진행중인 프로젝트 목록</h1>
    <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
        <div class="col" th:each="project : ${list}">
            <div class="card">
                <div class="card-header bg-warning text-dark">
                    <h5 class="card-title" th:text="${project.name}">프로젝트 이름</h5>
                </div>
                <div class="card-body">
                    <blockquote class="blockquote mb-0">
                        <p th:text="${project.description}">프로젝트 설명</p>
                        <footer class="blockquote-footer">
                            <cite title="Started At" th:text="${project.startAt}">시작일</cite> ~ 
                            <cite title="Ended At" th:text="${project.endAt}">종료일</cite>
                        </footer>
                    </blockquote>
                </div>
                <div class="card-footer text-muted">
                    팀장 : <span th:text="${project.maintainer.username}">팀장</span>
                    <form action="/project/delete" class="btn btn-danger btn-sm float-end me-2" th:method="DELETE">
                    	<input type="hidden" name="id" th:value="${ project.id }" />
                    	<button class="btn btn-danger">Delete</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</main>
</html>
